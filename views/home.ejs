<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <h3>Stable Diffusion Project</h3>
    <form action="/" method="post">
        <label for="prompt">Enter Prompt for Stable Diffusion: </label>
        <input type="text" id="prompt" name="prompt">
        <input type="submit" value="Submit">
    </form>

    <% if(display){ %>
        <img src="data:image/jpeg;base64,<%= image %>" alt="<%= prompt %>">
    <% } %>
</body>
</html>
